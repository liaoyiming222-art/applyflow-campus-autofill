# 参与贡献

感谢你帮助改进 ApplyFlow。

## 开发原则

- 只自动填写用户档案中明确存在的内容，不猜测主观选择。
- 不点击招聘网站的保存、隐私同意或提交按钮。
- 实习、工作、项目、教育、外语和奖励必须保持独立的数据与页面上下文。
- 新增网站兼容逻辑时，应同时补充不依赖真实账号和个人数据的测试夹具。
- 测试资料必须使用虚构身份和虚构证件信息。

## 本地验证

1. 安装 Node.js 20 或更高版本。
2. 运行 `npm install`。
3. 运行 `npm test` 检查简历解析和北森式表单回归。
4. 在 Edge 的 `edge://extensions/` 中加载项目目录，进行人工验证。

提交代码前请确认扩展不会自动保存或提交招聘申请，也不要提交真实简历、账号信息、Cookie 或页面接口响应。

